#include <stdio.h>
int main()
{
    int n;
    int a;
    int b;
    scanf("%d", &n);
    int ai[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ai[i]);
        b +
    }
    for (int i = 0; i < n; i++)
    {
        if (b)
        {
            /* code */
        }
        
    }
    printf("%d", b);
    return 0;
}